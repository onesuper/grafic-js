saveFrame("output.png")

size(400, 400);


stroke(255); //outline color
background(192, 64, 0); //background
fill(150)//shape color

mousePressed();

line(150, 25, 270, 350);

rectMode(CENTER);
rect(x, y, width, height);

ellipseMode(CORNER);
ellipse(x, y, width, height);
